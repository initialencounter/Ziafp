[package]
name = "Ziafp-client"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = {version = "0.12.8", features = ["blocking", "json", "multipart"]}
tokio = { version = "1.41.0", features = ["full"] }
winreg = "0.52.0"
